// =============================================================
// Auto-generated testbench for ibex_load_store_unit_trojan_DoS
// Generated by RV-TroGen - fully generic port-driven TB
// Works for ANY processor module without modification
// =============================================================
`timescale 1ns/1ps

module tb_ibex_load_store_unit_trojan_DoS;
    

    // ── Clock & Reset ────────────────────────────────────────────────
    logic clk_i;
    logic rst_ni;

    // ── Input signals ────────────────────────────────────────────────
    logic data_gnt_i;
    logic data_rvalid_i;
    logic data_bus_err_i;
    logic data_pmp_err_i;
    logic [MemDataWidth-1:0] data_rdata_i;
    logic lsu_we_i;
    logic [1:0] lsu_type_i;
    logic [31:0] lsu_wdata_i;
    logic lsu_sign_ext_i;
    logic lsu_req_i;
    logic [31:0] adder_result_ex_i;

    // ── Output signals ───────────────────────────────────────────────
    logic data_req_o;
    logic [31:0] data_addr_o;
    logic data_we_o;
    logic [3:0] data_be_o;
    logic [MemDataWidth-1:0] data_wdata_o;
    logic [31:0] lsu_rdata_o;
    logic lsu_rdata_valid_o;
    logic addr_incr_req_o;
    logic [31:0] addr_last_o;
    logic lsu_req_done_o;
    logic lsu_resp_valid_o;
    logic load_err_o;
    logic load_resp_intg_err_o;
    logic store_err_o;
    logic store_resp_intg_err_o;
    logic busy_o;
    logic perf_load_o;
    logic perf_store_o;

    // ── DUT ─────────────────────────────────────────────────────────────
    ibex_load_store_unit_trojan_DoS dut (
        .clk_i(clk_i),
        .rst_ni(rst_ni),
        .data_gnt_i(data_gnt_i),
        .data_rvalid_i(data_rvalid_i),
        .data_bus_err_i(data_bus_err_i),
        .data_pmp_err_i(data_pmp_err_i),
        .data_rdata_i(data_rdata_i),
        .lsu_we_i(lsu_we_i),
        .lsu_type_i(lsu_type_i),
        .lsu_wdata_i(lsu_wdata_i),
        .lsu_sign_ext_i(lsu_sign_ext_i),
        .lsu_req_i(lsu_req_i),
        .adder_result_ex_i(adder_result_ex_i),
        .data_req_o(data_req_o),
        .data_addr_o(data_addr_o),
        .data_we_o(data_we_o),
        .data_be_o(data_be_o),
        .data_wdata_o(data_wdata_o),
        .lsu_rdata_o(lsu_rdata_o),
        .lsu_rdata_valid_o(lsu_rdata_valid_o),
        .addr_incr_req_o(addr_incr_req_o),
        .addr_last_o(addr_last_o),
        .lsu_req_done_o(lsu_req_done_o),
        .lsu_resp_valid_o(lsu_resp_valid_o),
        .load_err_o(load_err_o),
        .load_resp_intg_err_o(load_resp_intg_err_o),
        .store_err_o(store_err_o),
        .store_resp_intg_err_o(store_resp_intg_err_o),
        .busy_o(busy_o),
        .perf_load_o(perf_load_o),
        .perf_store_o(perf_store_o)
    );

    // ── Clock ────────────────────────────────────────────────────────────
    initial begin clk_i = 1'b0; forever #5 clk_i = ~clk_i; end

    // ── VCD Dump ─────────────────────────────────────────────────────────
    initial begin
        $dumpfile("ibex_load_store_unit_trojan_DoS.vcd");
        $dumpvars(0, tb_ibex_load_store_unit_trojan_DoS);
    end

    // ── Stimulus ─────────────────────────────────────────────────────────
    initial begin
        // 1. Initialize all inputs
        rst_ni = 1'b0;
        data_gnt_i = 1'b0;
        data_rvalid_i = 1'b0;
        data_bus_err_i = 1'b0;
        data_pmp_err_i = 1'b0;
        data_rdata_i = '0;
        lsu_we_i = 1'b0;
        lsu_type_i = '0;
        lsu_wdata_i = '0;
        lsu_sign_ext_i = 1'b0;
        lsu_req_i = 1'b0;
        adder_result_ex_i = '0;
        // 2. Release reset
        #100;
        rst_ni = 1'b1;
        #20;


        // Run 30,000 cycles of randomized stimulus
        //    Ensures trojan trigger counters (5000–25000) always activate
        // Drive inputs each cycle to exercise all trigger conditions
        // csr_op_i=CSR_OP_WRITE makes csr_wr=1 -> csr_we_int toggles
        repeat (30000) begin
            @(posedge clk_i);
            data_rvalid_i = 1'b1;
            data_rdata_i = $urandom;
            lsu_we_i = 1'b1;
            lsu_wdata_i = $urandom;
            lsu_req_i = 1'b1;
            @(posedge clk_i);
            data_rvalid_i = 1'b0;
            lsu_we_i = 1'b0;
            lsu_req_i = 1'b0;
        end

        $display("[%0t] Simulation complete: 30,000 stimulus cycles", $time);
        #100;
        $finish;
    end

    // ── Monitor (log observable output changes) ──────────────────────────
    always @(posedge clk_i) begin
        if (rst_ni && data_rvalid_i)
            $display("[%0t] active: data_addr_o=%0h", $time, data_addr_o);
    end

endmodule
// =============================================================
// Port summary (parsed from original RTL):
//   Inputs:  13
//   Outputs: 18
//   Packages: none
// =============================================================
