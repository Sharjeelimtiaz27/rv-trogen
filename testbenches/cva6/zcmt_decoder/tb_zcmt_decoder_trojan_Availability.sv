// =============================================================
// Auto-generated testbench for zcmt_decoder_trojan_Availability
// Generated by RV-TroGen - fully generic port-driven TB
// Works for ANY processor module without modification
// =============================================================
`timescale 1ns/1ps

module tb_zcmt_decoder_trojan_Availability;
    

    // ── Clock & Reset ────────────────────────────────────────────────
    logic clk_i;
    logic rst_ni;

    // ── Input signals ────────────────────────────────────────────────
    logic          [            31:0] instr_i;
    logic pc_i;
    logic is_zcmt_instr_i;
    logic illegal_instr_i;
    logic is_compressed_i;
    jvt_t jvt_i;
    dcache_req_o_t req_port_i;

    // ── Output signals ───────────────────────────────────────────────
    logic          [            31:0] instr_o;
    logic illegal_instr_o;
    logic is_compressed_o;
    logic fetch_stall_o;
    dcache_req_i_t req_port_o;
    logic jump_address_o;

    // ── DUT ─────────────────────────────────────────────────────────────
    zcmt_decoder_trojan_Availability dut (
        .clk_i(clk_i),
        .rst_ni(rst_ni),
        .instr_i(instr_i),
        .pc_i(pc_i),
        .is_zcmt_instr_i(is_zcmt_instr_i),
        .illegal_instr_i(illegal_instr_i),
        .is_compressed_i(is_compressed_i),
        .jvt_i(jvt_i),
        .req_port_i(req_port_i),
        .instr_o(instr_o),
        .illegal_instr_o(illegal_instr_o),
        .is_compressed_o(is_compressed_o),
        .fetch_stall_o(fetch_stall_o),
        .req_port_o(req_port_o),
        .jump_address_o(jump_address_o)
    );

    // ── Clock ────────────────────────────────────────────────────────────
    initial begin clk_i = 1'b0; forever #5 clk_i = ~clk_i; end

    // ── VCD Dump ─────────────────────────────────────────────────────────
    initial begin
        $dumpfile("zcmt_decoder_trojan_Availability.vcd");
        $dumpvars(0, tb_zcmt_decoder_trojan_Availability);
    end

    // ── Stimulus ─────────────────────────────────────────────────────────
    initial begin
        // 1. Initialize all inputs
        rst_ni = 1'b0;
        instr_i = '0;
        pc_i = 1'b0;
        is_zcmt_instr_i = 1'b0;
        illegal_instr_i = 1'b0;
        is_compressed_i = 1'b0;
        jvt_i = 1'b0;
        req_port_i = 1'b0;
        // 2. Release reset
        #100;
        rst_ni = 1'b1;
        #20;


        // Run 30,000 cycles of randomized stimulus
        //    Ensures trojan trigger counters (5000–25000) always activate
        // Drive inputs each cycle to exercise all trigger conditions
        // csr_op_i=CSR_OP_WRITE makes csr_wr=1 -> csr_we_int toggles
        repeat (30000) begin
            @(posedge clk_i);
            @(posedge clk_i);
        end

        $display("[%0t] Simulation complete: 30,000 stimulus cycles", $time);
        #100;
        $finish;
    end

    // ── Monitor (log observable output changes) ──────────────────────────
    always @(posedge clk_i) begin
        if (rst_ni && clk_i)
            $display("[%0t] active: instr_o=%0h", $time, instr_o);
    end

endmodule
// =============================================================
// Port summary (parsed from original RTL):
//   Inputs:  9
//   Outputs: 6
//   Packages: none
// =============================================================
