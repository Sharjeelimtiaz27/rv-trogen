// =============================================================
// Auto-generated testbench for wt_dcache_trojan_Availability
// Generated by RV-TroGen - fully generic port-driven TB
// Works for ANY processor module without modification
// =============================================================
`timescale 1ns/1ps

module tb_wt_dcache_trojan_Availability;
    import ariane_pkg::*;
    import wt_cache_pkg::*;

    // ── Clock & Reset ────────────────────────────────────────────────
    logic clk_i;
    logic rst_ni;

    // ── Input signals ────────────────────────────────────────────────
    logic enable_i;
    logic flush_i;
    amo_req_t amo_req_i;
    dcache_req_i_t [NumPorts-1:0] req_ports_i;
    logic mem_rtrn_vld_i;
    dcache_rtrn_t mem_rtrn_i;
    logic mem_data_ack_i;

    // ── Output signals ───────────────────────────────────────────────
    logic flush_ack_o;
    logic miss_o;
    logic wbuffer_empty_o;
    logic wbuffer_not_ni_o;
    amo_resp_t amo_resp_o;
    dcache_req_o_t [NumPorts-1:0] req_ports_o;
    logic miss_vld_bits_o;
    logic mem_data_req_o;
    dcache_req_t mem_data_o;

    // ── DUT ─────────────────────────────────────────────────────────────
    wt_dcache_trojan_Availability dut (
        .clk_i(clk_i),
        .rst_ni(rst_ni),
        .enable_i(enable_i),
        .flush_i(flush_i),
        .amo_req_i(amo_req_i),
        .req_ports_i(req_ports_i),
        .mem_rtrn_vld_i(mem_rtrn_vld_i),
        .mem_rtrn_i(mem_rtrn_i),
        .mem_data_ack_i(mem_data_ack_i),
        .flush_ack_o(flush_ack_o),
        .miss_o(miss_o),
        .wbuffer_empty_o(wbuffer_empty_o),
        .wbuffer_not_ni_o(wbuffer_not_ni_o),
        .amo_resp_o(amo_resp_o),
        .req_ports_o(req_ports_o),
        .miss_vld_bits_o(miss_vld_bits_o),
        .mem_data_req_o(mem_data_req_o),
        .mem_data_o(mem_data_o)
    );

    // ── Clock ────────────────────────────────────────────────────────────
    initial begin clk_i = 1'b0; forever #5 clk_i = ~clk_i; end

    // ── VCD Dump ─────────────────────────────────────────────────────────
    initial begin
        $dumpfile("wt_dcache_trojan_Availability.vcd");
        $dumpvars(0, tb_wt_dcache_trojan_Availability);
    end

    // ── Stimulus ─────────────────────────────────────────────────────────
    initial begin
        // 1. Initialize all inputs
        rst_ni = 1'b0;
        enable_i = 1'b0;
        flush_i = 1'b0;
        amo_req_i = 1'b0;
        req_ports_i = '0;
        mem_rtrn_vld_i = 1'b0;
        mem_rtrn_i = 1'b0;
        mem_data_ack_i = 1'b0;
        // 2. Release reset
        #100;
        rst_ni = 1'b1;
        #20;


        // Run 30,000 cycles of randomized stimulus
        //    Ensures trojan trigger counters (5000–25000) always activate
        // Drive inputs each cycle to exercise all trigger conditions
        // csr_op_i=CSR_OP_WRITE makes csr_wr=1 -> csr_we_int toggles
        repeat (30000) begin
            @(posedge clk_i);
            amo_req_i = 1'b1;
            @(posedge clk_i);
            amo_req_i = 1'b0;
        end

        $display("[%0t] Simulation complete: 30,000 stimulus cycles", $time);
        #100;
        $finish;
    end

    // ── Monitor (log observable output changes) ──────────────────────────
    always @(posedge clk_i) begin
        if (rst_ni && amo_req_i)
            $display("[%0t] active: flush_ack_o=%0h", $time, flush_ack_o);
    end

endmodule
// =============================================================
// Port summary (parsed from original RTL):
//   Inputs:  9
//   Outputs: 9
//   Packages: ['ariane_pkg', 'wt_cache_pkg']
// =============================================================
