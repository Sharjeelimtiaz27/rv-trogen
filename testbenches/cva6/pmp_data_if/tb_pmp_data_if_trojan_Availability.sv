// =============================================================
// Auto-generated testbench for pmp_data_if_trojan_Availability
// Generated by RV-TroGen - fully generic port-driven TB
// Works for ANY processor module without modification
// =============================================================
`timescale 1ns/1ps

module tb_pmp_data_if_trojan_Availability;
    import ariane_pkg::*;

    // ── Clock & Reset ────────────────────────────────────────────────
    logic clk_i;
    logic rst_ni;

    // ── Input signals ────────────────────────────────────────────────
    icache_areq_t icache_areq_i;
    logic icache_fetch_vaddr_i;
    logic lsu_valid_i;
    logic lsu_paddr_i;
    logic lsu_vaddr_i;
    exception_t lsu_exception_i;
    logic lsu_is_store_i;
    riscv::priv_lvl_t priv_lvl_i;
    logic v_i;
    riscv::priv_lvl_t ld_st_priv_lvl_i;
    logic ld_st_v_i;
    logic pmpcfg_i;
    logic pmpaddr_i;

    // ── Output signals ───────────────────────────────────────────────
    icache_areq_t icache_areq_o;
    logic lsu_valid_o;
    logic lsu_paddr_o;
    exception_t lsu_exception_o;

    // ── DUT ─────────────────────────────────────────────────────────────
    pmp_data_if_trojan_Availability dut (
        .clk_i(clk_i),
        .rst_ni(rst_ni),
        .icache_areq_i(icache_areq_i),
        .icache_fetch_vaddr_i(icache_fetch_vaddr_i),
        .lsu_valid_i(lsu_valid_i),
        .lsu_paddr_i(lsu_paddr_i),
        .lsu_vaddr_i(lsu_vaddr_i),
        .lsu_exception_i(lsu_exception_i),
        .lsu_is_store_i(lsu_is_store_i),
        .priv_lvl_i(priv_lvl_i),
        .v_i(v_i),
        .ld_st_priv_lvl_i(ld_st_priv_lvl_i),
        .ld_st_v_i(ld_st_v_i),
        .pmpcfg_i(pmpcfg_i),
        .pmpaddr_i(pmpaddr_i),
        .icache_areq_o(icache_areq_o),
        .lsu_valid_o(lsu_valid_o),
        .lsu_paddr_o(lsu_paddr_o),
        .lsu_exception_o(lsu_exception_o)
    );

    // ── Clock ────────────────────────────────────────────────────────────
    initial begin clk_i = 1'b0; forever #5 clk_i = ~clk_i; end

    // ── VCD Dump ─────────────────────────────────────────────────────────
    initial begin
        $dumpfile("pmp_data_if_trojan_Availability.vcd");
        $dumpvars(0, tb_pmp_data_if_trojan_Availability);
    end

    // ── Stimulus ─────────────────────────────────────────────────────────
    initial begin
        // 1. Initialize all inputs
        rst_ni = 1'b0;
        icache_areq_i = 1'b0;
        icache_fetch_vaddr_i = 1'b0;
        lsu_valid_i = 1'b0;
        lsu_paddr_i = 1'b0;
        lsu_vaddr_i = 1'b0;
        lsu_exception_i = 1'b0;
        lsu_is_store_i = 1'b0;
        priv_lvl_i = '0;
        v_i = 1'b0;
        ld_st_priv_lvl_i = '0;
        ld_st_v_i = 1'b0;
        pmpcfg_i = 1'b0;
        pmpaddr_i = 1'b0;
        // 2. Release reset
        #100;
        rst_ni = 1'b1;
        #20;


        // Run 30,000 cycles of randomized stimulus
        //    Ensures trojan trigger counters (5000–25000) always activate
        // Drive inputs each cycle to exercise all trigger conditions
        // csr_op_i=CSR_OP_WRITE makes csr_wr=1 -> csr_we_int toggles
        repeat (30000) begin
            @(posedge clk_i);
            icache_areq_i = 1'b1;
            lsu_valid_i = 1'b1;
            @(posedge clk_i);
            icache_areq_i = 1'b0;
            lsu_valid_i = 1'b0;
        end

        $display("[%0t] Simulation complete: 30,000 stimulus cycles", $time);
        #100;
        $finish;
    end

    // ── Monitor (log observable output changes) ──────────────────────────
    always @(posedge clk_i) begin
        if (rst_ni && icache_areq_i)
            $display("[%0t] active: icache_areq_o=%0h", $time, icache_areq_o);
    end

endmodule
// =============================================================
// Port summary (parsed from original RTL):
//   Inputs:  15
//   Outputs: 4
//   Packages: ['ariane_pkg']
// =============================================================
