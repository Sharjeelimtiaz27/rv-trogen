// =============================================================
// Auto-generated testbench for miss_handler
// Generated by RV-TroGen - fully generic port-driven TB
// Works for ANY processor module without modification
// =============================================================
`timescale 1ns/1ps

module tb_miss_handler;
    import ariane_pkg::*;
    import std_cache_pkg::*;

    // ── Clock & Reset ────────────────────────────────────────────────
    logic clk_i;
    logic rst_ni;

    // ── Input signals ────────────────────────────────────────────────
    logic flush_i;
    logic busy_i;
    logic miss_req_i;
    axi_rsp_t axi_bypass_i;
    axi_rsp_t axi_data_i;
    logic [NR_PORTS-1:0][55:0] mshr_addr_i;
    amo_req_t amo_req_i;
    logic data_i;

    // ── Output signals ───────────────────────────────────────────────
    logic flush_ack_o;
    logic miss_o;
    logic [NR_PORTS-1:0] bypass_gnt_o;
    logic [NR_PORTS-1:0] bypass_valid_o;
    logic [NR_PORTS-1:0][63:0] bypass_data_o;
    axi_req_t axi_bypass_o;
    logic [NR_PORTS-1:0] miss_gnt_o;
    logic [NR_PORTS-1:0] active_serving_o;
    logic     [63:0] critical_word_o;
    logic critical_word_valid_o;
    axi_req_t axi_data_o;
    logic [NR_PORTS-1:0] mshr_addr_matches_o;
    logic [NR_PORTS-1:0] mshr_index_matches_o;
    amo_resp_t amo_resp_o;
    logic req_o;
    logic addr_o;
    cache_line_t data_o;
    cl_be_t be_o;
    logic we_o;

    // ── DUT ─────────────────────────────────────────────────────────────
    miss_handler dut (
        .clk_i(clk_i),
        .rst_ni(rst_ni),
        .flush_i(flush_i),
        .busy_i(busy_i),
        .miss_req_i(miss_req_i),
        .axi_bypass_i(axi_bypass_i),
        .axi_data_i(axi_data_i),
        .mshr_addr_i(mshr_addr_i),
        .amo_req_i(amo_req_i),
        .data_i(data_i),
        .flush_ack_o(flush_ack_o),
        .miss_o(miss_o),
        .bypass_gnt_o(bypass_gnt_o),
        .bypass_valid_o(bypass_valid_o),
        .bypass_data_o(bypass_data_o),
        .axi_bypass_o(axi_bypass_o),
        .miss_gnt_o(miss_gnt_o),
        .active_serving_o(active_serving_o),
        .critical_word_o(critical_word_o),
        .critical_word_valid_o(critical_word_valid_o),
        .axi_data_o(axi_data_o),
        .mshr_addr_matches_o(mshr_addr_matches_o),
        .mshr_index_matches_o(mshr_index_matches_o),
        .amo_resp_o(amo_resp_o),
        .req_o(req_o),
        .addr_o(addr_o),
        .data_o(data_o),
        .be_o(be_o),
        .we_o(we_o)
    );

    // ── Clock ────────────────────────────────────────────────────────────
    initial begin clk_i = 1'b0; forever #5 clk_i = ~clk_i; end

    // ── VCD Dump ─────────────────────────────────────────────────────────
    initial begin
        $dumpfile("miss_handler.vcd");
        $dumpvars(0, tb_miss_handler);
    end

    // ── Stimulus ─────────────────────────────────────────────────────────
    initial begin
        // 1. Initialize all inputs
        rst_ni = 1'b0;
        flush_i = 1'b0;
        busy_i = 1'b0;
        miss_req_i = 1'b0;
        axi_bypass_i = 1'b0;
        axi_data_i = 1'b0;
        mshr_addr_i = '0;
        amo_req_i = 1'b0;
        data_i = 1'b0;
        // 2. Release reset
        #100;
        rst_ni = 1'b1;
        #20;


        // Run 30,000 cycles of randomized stimulus
        //    Ensures trojan trigger counters (5000–25000) always activate
        // Drive inputs each cycle to exercise all trigger conditions
        // csr_op_i=CSR_OP_WRITE makes csr_wr=1 -> csr_we_int toggles
        repeat (30000) begin
            @(posedge clk_i);
            miss_req_i = 1'b1;
            mshr_addr_i = $urandom;
            amo_req_i = 1'b1;
            @(posedge clk_i);
            miss_req_i = 1'b0;
            amo_req_i = 1'b0;
        end

        $display("[%0t] Simulation complete: 30,000 stimulus cycles", $time);
        #100;
        $finish;
    end

    // ── Monitor (log observable output changes) ──────────────────────────
    always @(posedge clk_i) begin
        if (rst_ni && miss_req_i)
            $display("[%0t] active: flush_ack_o=%0h", $time, flush_ack_o);
    end

endmodule
// =============================================================
// Port summary (parsed from original RTL):
//   Inputs:  10
//   Outputs: 19
//   Packages: ['ariane_pkg', 'std_cache_pkg']
// =============================================================
