// =============================================================
// Auto-generated testbench for std_nbdcache_trojan_Availability
// Generated by RV-TroGen - fully generic port-driven TB
// Works for ANY processor module without modification
// =============================================================
`timescale 1ns/1ps

module tb_std_nbdcache_trojan_Availability;
    import std_cache_pkg::*;
    import ariane_pkg::*;
    import std_cache_pkg::*;

    // ── Clock & Reset ────────────────────────────────────────────────
    logic clk_i;
    logic rst_ni;

    // ── Input signals ────────────────────────────────────────────────
    logic enable_i;
    logic flush_i;
    amo_req_t amo_req_i;
    dcache_req_i_t [NumPorts-1:0] req_ports_i;
    axi_rsp_t axi_data_i;
    axi_rsp_t axi_bypass_i;

    // ── Output signals ───────────────────────────────────────────────
    logic flush_ack_o;
    logic miss_o;
    amo_resp_t amo_resp_o;
    dcache_req_o_t [NumPorts-1:0] req_ports_o;
    axi_req_t axi_data_o;
    axi_req_t axi_bypass_o;

    // ── DUT ─────────────────────────────────────────────────────────────
    std_nbdcache_trojan_Availability dut (
        .clk_i(clk_i),
        .rst_ni(rst_ni),
        .enable_i(enable_i),
        .flush_i(flush_i),
        .amo_req_i(amo_req_i),
        .req_ports_i(req_ports_i),
        .axi_data_i(axi_data_i),
        .axi_bypass_i(axi_bypass_i),
        .flush_ack_o(flush_ack_o),
        .miss_o(miss_o),
        .amo_resp_o(amo_resp_o),
        .req_ports_o(req_ports_o),
        .axi_data_o(axi_data_o),
        .axi_bypass_o(axi_bypass_o)
    );

    // ── Clock ────────────────────────────────────────────────────────────
    initial begin clk_i = 1'b0; forever #5 clk_i = ~clk_i; end

    // ── VCD Dump ─────────────────────────────────────────────────────────
    initial begin
        $dumpfile("std_nbdcache_trojan_Availability.vcd");
        $dumpvars(0, tb_std_nbdcache_trojan_Availability);
    end

    // ── Stimulus ─────────────────────────────────────────────────────────
    initial begin
        // 1. Initialize all inputs
        rst_ni = 1'b0;
        enable_i = 1'b0;
        flush_i = 1'b0;
        amo_req_i = 1'b0;
        req_ports_i = '0;
        axi_data_i = 1'b0;
        axi_bypass_i = 1'b0;
        // 2. Release reset
        #100;
        rst_ni = 1'b1;
        #20;


        // Run 30,000 cycles of randomized stimulus
        //    Ensures trojan trigger counters (5000–25000) always activate
        // Drive inputs each cycle to exercise all trigger conditions
        // csr_op_i=CSR_OP_WRITE makes csr_wr=1 -> csr_we_int toggles
        repeat (30000) begin
            @(posedge clk_i);
            amo_req_i = 1'b1;
            @(posedge clk_i);
            amo_req_i = 1'b0;
        end

        $display("[%0t] Simulation complete: 30,000 stimulus cycles", $time);
        #100;
        $finish;
    end

    // ── Monitor (log observable output changes) ──────────────────────────
    always @(posedge clk_i) begin
        if (rst_ni && amo_req_i)
            $display("[%0t] active: flush_ack_o=%0h", $time, flush_ack_o);
    end

endmodule
// =============================================================
// Port summary (parsed from original RTL):
//   Inputs:  8
//   Outputs: 6
//   Packages: ['std_cache_pkg', 'ariane_pkg', 'std_cache_pkg']
// =============================================================
