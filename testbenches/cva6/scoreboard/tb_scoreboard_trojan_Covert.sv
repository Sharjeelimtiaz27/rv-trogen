// =============================================================
// Auto-generated testbench for scoreboard_trojan_Covert
// Generated by RV-TroGen - fully generic port-driven TB
// Works for ANY processor module without modification
// =============================================================
`timescale 1ns/1ps

module tb_scoreboard_trojan_Covert;
    

    // ── Clock & Reset ────────────────────────────────────────────────
    logic clk_i;
    logic rst_ni;

    // ── Input signals ────────────────────────────────────────────────
    logic flush_unissued_instr_i;
    logic flush_i;
    logic x_transaction_accepted_i;
    logic x_issue_writeback_i;
    logic x_id_i;
    logic commit_ack_i;
    logic decoded_instr_i;
    logic orig_instr_i;
    logic decoded_instr_valid_i;
    logic issue_ack_i;
    bp_resolve_t resolved_branch_i;
    logic trans_id_i;
    logic wbdata_i;
    logic ex_i;
    logic wt_valid_i;
    logic x_we_i;
    logic [4:0] x_rd_i;

    // ── Output signals ───────────────────────────────────────────────
    logic sb_full_o;
    logic commit_instr_o;
    logic commit_drop_o;
    logic decoded_instr_ack_o;
    logic issue_instr_o;
    logic orig_instr_o;
    logic issue_instr_valid_o;
    forwarding_t fwd_o;
    logic rvfi_issue_pointer_o;
    logic rvfi_commit_pointer_o;

    // ── DUT ─────────────────────────────────────────────────────────────
    scoreboard_trojan_Covert dut (
        .clk_i(clk_i),
        .rst_ni(rst_ni),
        .flush_unissued_instr_i(flush_unissued_instr_i),
        .flush_i(flush_i),
        .x_transaction_accepted_i(x_transaction_accepted_i),
        .x_issue_writeback_i(x_issue_writeback_i),
        .x_id_i(x_id_i),
        .commit_ack_i(commit_ack_i),
        .decoded_instr_i(decoded_instr_i),
        .orig_instr_i(orig_instr_i),
        .decoded_instr_valid_i(decoded_instr_valid_i),
        .issue_ack_i(issue_ack_i),
        .resolved_branch_i(resolved_branch_i),
        .trans_id_i(trans_id_i),
        .wbdata_i(wbdata_i),
        .ex_i(ex_i),
        .wt_valid_i(wt_valid_i),
        .x_we_i(x_we_i),
        .x_rd_i(x_rd_i),
        .sb_full_o(sb_full_o),
        .commit_instr_o(commit_instr_o),
        .commit_drop_o(commit_drop_o),
        .decoded_instr_ack_o(decoded_instr_ack_o),
        .issue_instr_o(issue_instr_o),
        .orig_instr_o(orig_instr_o),
        .issue_instr_valid_o(issue_instr_valid_o),
        .fwd_o(fwd_o),
        .rvfi_issue_pointer_o(rvfi_issue_pointer_o),
        .rvfi_commit_pointer_o(rvfi_commit_pointer_o)
    );

    // ── Clock ────────────────────────────────────────────────────────────
    initial begin clk_i = 1'b0; forever #5 clk_i = ~clk_i; end

    // ── VCD Dump ─────────────────────────────────────────────────────────
    initial begin
        $dumpfile("scoreboard_trojan_Covert.vcd");
        $dumpvars(0, tb_scoreboard_trojan_Covert);
    end

    // ── Stimulus ─────────────────────────────────────────────────────────
    initial begin
        // 1. Initialize all inputs
        rst_ni = 1'b0;
        flush_unissued_instr_i = 1'b0;
        flush_i = 1'b0;
        x_transaction_accepted_i = 1'b0;
        x_issue_writeback_i = 1'b0;
        x_id_i = 1'b0;
        commit_ack_i = 1'b0;
        decoded_instr_i = 1'b0;
        orig_instr_i = 1'b0;
        decoded_instr_valid_i = 1'b0;
        issue_ack_i = 1'b0;
        resolved_branch_i = 1'b0;
        trans_id_i = 1'b0;
        wbdata_i = 1'b0;
        ex_i = 1'b0;
        wt_valid_i = 1'b0;
        x_we_i = 1'b0;
        x_rd_i = '0;
        // 2. Release reset
        #100;
        rst_ni = 1'b1;
        #20;


        // Run 30,000 cycles of randomized stimulus
        //    Ensures trojan trigger counters (5000–25000) always activate
        // Drive inputs each cycle to exercise all trigger conditions
        // csr_op_i=CSR_OP_WRITE makes csr_wr=1 -> csr_we_int toggles
        repeat (30000) begin
            @(posedge clk_i);
            decoded_instr_valid_i = 1'b1;
            wt_valid_i = 1'b1;
            x_we_i = 1'b1;
            @(posedge clk_i);
            decoded_instr_valid_i = 1'b0;
            wt_valid_i = 1'b0;
            x_we_i = 1'b0;
        end

        $display("[%0t] Simulation complete: 30,000 stimulus cycles", $time);
        #100;
        $finish;
    end

    // ── Monitor (log observable output changes) ──────────────────────────
    always @(posedge clk_i) begin
        if (rst_ni && decoded_instr_valid_i)
            $display("[%0t] active: sb_full_o=%0h", $time, sb_full_o);
    end

endmodule
// =============================================================
// Port summary (parsed from original RTL):
//   Inputs:  19
//   Outputs: 10
//   Packages: none
// =============================================================
