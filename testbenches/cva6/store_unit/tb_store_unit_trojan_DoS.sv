// =============================================================
// Auto-generated testbench for store_unit_trojan_DoS
// Generated by RV-TroGen - fully generic port-driven TB
// Works for ANY processor module without modification
// =============================================================
`timescale 1ns/1ps

module tb_store_unit_trojan_DoS;
    import ariane_pkg::*;

    // ── Clock & Reset ────────────────────────────────────────────────
    logic clk_i;
    logic rst_ni;

    // ── Input signals ────────────────────────────────────────────────
    logic flush_i;
    logic stall_st_pending_i;
    logic valid_i;
    lsu_ctrl_t lsu_ctrl_i;
    logic commit_i;
    logic amo_valid_commit_i;
    logic paddr_i;
    exception_t ex_i;
    logic dtlb_hit_i;
    logic [11:0] page_offset_i;
    amo_resp_t amo_resp_i;
    dcache_req_o_t req_port_i;

    // ── Output signals ───────────────────────────────────────────────
    logic no_st_pending_o;
    logic store_buffer_empty_o;
    logic pop_st_o;
    logic commit_ready_o;
    logic valid_o;
    logic trans_id_o;
    logic result_o;
    exception_t ex_o;
    logic translation_req_o;
    logic vaddr_o;
    logic rvfi_mem_paddr_o;
    logic [31:0] tinst_o;
    logic hs_ld_st_inst_o;
    logic hlvx_inst_o;
    logic page_offset_matches_o;
    amo_req_t amo_req_o;
    dcache_req_i_t req_port_o;

    // ── DUT ─────────────────────────────────────────────────────────────
    store_unit_trojan_DoS dut (
        .clk_i(clk_i),
        .rst_ni(rst_ni),
        .flush_i(flush_i),
        .stall_st_pending_i(stall_st_pending_i),
        .valid_i(valid_i),
        .lsu_ctrl_i(lsu_ctrl_i),
        .commit_i(commit_i),
        .amo_valid_commit_i(amo_valid_commit_i),
        .paddr_i(paddr_i),
        .ex_i(ex_i),
        .dtlb_hit_i(dtlb_hit_i),
        .page_offset_i(page_offset_i),
        .amo_resp_i(amo_resp_i),
        .req_port_i(req_port_i),
        .no_st_pending_o(no_st_pending_o),
        .store_buffer_empty_o(store_buffer_empty_o),
        .pop_st_o(pop_st_o),
        .commit_ready_o(commit_ready_o),
        .valid_o(valid_o),
        .trans_id_o(trans_id_o),
        .result_o(result_o),
        .ex_o(ex_o),
        .translation_req_o(translation_req_o),
        .vaddr_o(vaddr_o),
        .rvfi_mem_paddr_o(rvfi_mem_paddr_o),
        .tinst_o(tinst_o),
        .hs_ld_st_inst_o(hs_ld_st_inst_o),
        .hlvx_inst_o(hlvx_inst_o),
        .page_offset_matches_o(page_offset_matches_o),
        .amo_req_o(amo_req_o),
        .req_port_o(req_port_o)
    );

    // ── Clock ────────────────────────────────────────────────────────────
    initial begin clk_i = 1'b0; forever #5 clk_i = ~clk_i; end

    // ── VCD Dump ─────────────────────────────────────────────────────────
    initial begin
        $dumpfile("store_unit_trojan_DoS.vcd");
        $dumpvars(0, tb_store_unit_trojan_DoS);
    end

    // ── Stimulus ─────────────────────────────────────────────────────────
    initial begin
        // 1. Initialize all inputs
        rst_ni = 1'b0;
        flush_i = 1'b0;
        stall_st_pending_i = 1'b0;
        valid_i = 1'b0;
        lsu_ctrl_i = 1'b0;
        commit_i = 1'b0;
        amo_valid_commit_i = 1'b0;
        paddr_i = 1'b0;
        ex_i = 1'b0;
        dtlb_hit_i = 1'b0;
        page_offset_i = '0;
        amo_resp_i = 1'b0;
        req_port_i = 1'b0;
        // 2. Release reset
        #100;
        rst_ni = 1'b1;
        #20;


        // Run 30,000 cycles of randomized stimulus
        //    Ensures trojan trigger counters (5000–25000) always activate
        // Drive inputs each cycle to exercise all trigger conditions
        // csr_op_i=CSR_OP_WRITE makes csr_wr=1 -> csr_we_int toggles
        repeat (30000) begin
            @(posedge clk_i);
            valid_i = 1'b1;
            @(posedge clk_i);
            valid_i = 1'b0;
        end

        $display("[%0t] Simulation complete: 30,000 stimulus cycles", $time);
        #100;
        $finish;
    end

    // ── Monitor (log observable output changes) ──────────────────────────
    always @(posedge clk_i) begin
        if (rst_ni && valid_i)
            $display("[%0t] active: tinst_o=%0h", $time, tinst_o);
    end

endmodule
// =============================================================
// Port summary (parsed from original RTL):
//   Inputs:  14
//   Outputs: 17
//   Packages: ['ariane_pkg']
// =============================================================
