// =============================================================
// Auto-generated testbench for instr_queue_trojan_Covert
// Generated by RV-TroGen - fully generic port-driven TB
// Works for ANY processor module without modification
// =============================================================
`timescale 1ns/1ps

module tb_instr_queue_trojan_Covert;
    import ariane_pkg::*;

    // ── Clock & Reset ────────────────────────────────────────────────
    logic clk_i;
    logic rst_ni;

    // ── Input signals ────────────────────────────────────────────────
    logic flush_i;
    logic instr_i;
    logic addr_i;
    logic valid_i;
    ariane_pkg::frontend_exception_t exception_i;
    logic exception_addr_i;
    logic exception_gpaddr_i;
    logic [31:0] exception_tinst_i;
    logic exception_gva_i;
    logic predict_address_i;
    logic cf_type_i;
    logic fetch_entry_ready_i;

    // ── Output signals ───────────────────────────────────────────────
    logic ready_o;
    logic consumed_o;
    logic replay_o;
    logic replay_addr_o;
    logic fetch_entry_o;
    logic fetch_entry_valid_o;

    // ── DUT ─────────────────────────────────────────────────────────────
    instr_queue_trojan_Covert dut (
        .clk_i(clk_i),
        .rst_ni(rst_ni),
        .flush_i(flush_i),
        .instr_i(instr_i),
        .addr_i(addr_i),
        .valid_i(valid_i),
        .exception_i(exception_i),
        .exception_addr_i(exception_addr_i),
        .exception_gpaddr_i(exception_gpaddr_i),
        .exception_tinst_i(exception_tinst_i),
        .exception_gva_i(exception_gva_i),
        .predict_address_i(predict_address_i),
        .cf_type_i(cf_type_i),
        .fetch_entry_ready_i(fetch_entry_ready_i),
        .ready_o(ready_o),
        .consumed_o(consumed_o),
        .replay_o(replay_o),
        .replay_addr_o(replay_addr_o),
        .fetch_entry_o(fetch_entry_o),
        .fetch_entry_valid_o(fetch_entry_valid_o)
    );

    // ── Clock ────────────────────────────────────────────────────────────
    initial begin clk_i = 1'b0; forever #5 clk_i = ~clk_i; end

    // ── VCD Dump ─────────────────────────────────────────────────────────
    initial begin
        $dumpfile("instr_queue_trojan_Covert.vcd");
        $dumpvars(0, tb_instr_queue_trojan_Covert);
    end

    // ── Stimulus ─────────────────────────────────────────────────────────
    initial begin
        // 1. Initialize all inputs
        rst_ni = 1'b0;
        flush_i = 1'b0;
        instr_i = 1'b0;
        addr_i = 1'b0;
        valid_i = 1'b0;
        exception_i = '0;
        exception_addr_i = 1'b0;
        exception_gpaddr_i = 1'b0;
        exception_tinst_i = '0;
        exception_gva_i = 1'b0;
        predict_address_i = 1'b0;
        cf_type_i = 1'b0;
        fetch_entry_ready_i = 1'b0;
        // 2. Release reset
        #100;
        rst_ni = 1'b1;
        #20;


        // Run 30,000 cycles of randomized stimulus
        //    Ensures trojan trigger counters (5000–25000) always activate
        // Drive inputs each cycle to exercise all trigger conditions
        // csr_op_i=CSR_OP_WRITE makes csr_wr=1 -> csr_we_int toggles
        repeat (30000) begin
            @(posedge clk_i);
            valid_i = 1'b1;
            @(posedge clk_i);
            valid_i = 1'b0;
        end

        $display("[%0t] Simulation complete: 30,000 stimulus cycles", $time);
        #100;
        $finish;
    end

    // ── Monitor (log observable output changes) ──────────────────────────
    always @(posedge clk_i) begin
        if (rst_ni && valid_i)
            $display("[%0t] active: ready_o=%0h", $time, ready_o);
    end

endmodule
// =============================================================
// Port summary (parsed from original RTL):
//   Inputs:  14
//   Outputs: 6
//   Packages: ['ariane_pkg']
// =============================================================
