// =============================================================
// Auto-generated testbench for cvxif_issue_register_commit_if_driver_trojan_DoS
// Generated by RV-TroGen - fully generic port-driven TB
// Works for ANY processor module without modification
// =============================================================
`timescale 1ns/1ps

module tb_cvxif_issue_register_commit_if_driver_trojan_DoS;
    

    // ── Clock & Reset ────────────────────────────────────────────────
    logic clk_i;
    logic rst_ni;

    // ── Input signals ────────────────────────────────────────────────
    logic flush_i;
    logic hart_id_i;
    logic issue_ready_i;
    x_issue_resp_t issue_resp_i;
    logic register_ready_i;
    logic valid_i;
    logic [31:0] x_off_instr_i;
    logic x_trans_id_i;
    logic register_i;
    logic rs_valid_i;

    // ── Output signals ───────────────────────────────────────────────
    logic issue_valid_o;
    x_issue_req_t issue_req_o;
    logic register_valid_o;
    x_register_t register_o;
    logic commit_valid_o;
    x_commit_t commit_o;

    // ── DUT ─────────────────────────────────────────────────────────────
    cvxif_issue_register_commit_if_driver_trojan_DoS dut (
        .clk_i(clk_i),
        .rst_ni(rst_ni),
        .flush_i(flush_i),
        .hart_id_i(hart_id_i),
        .issue_ready_i(issue_ready_i),
        .issue_resp_i(issue_resp_i),
        .register_ready_i(register_ready_i),
        .valid_i(valid_i),
        .x_off_instr_i(x_off_instr_i),
        .x_trans_id_i(x_trans_id_i),
        .register_i(register_i),
        .rs_valid_i(rs_valid_i),
        .issue_valid_o(issue_valid_o),
        .issue_req_o(issue_req_o),
        .register_valid_o(register_valid_o),
        .register_o(register_o),
        .commit_valid_o(commit_valid_o),
        .commit_o(commit_o)
    );

    // ── Clock ────────────────────────────────────────────────────────────
    initial begin clk_i = 1'b0; forever #5 clk_i = ~clk_i; end

    // ── VCD Dump ─────────────────────────────────────────────────────────
    initial begin
        $dumpfile("cvxif_issue_register_commit_if_driver_trojan_DoS.vcd");
        $dumpvars(0, tb_cvxif_issue_register_commit_if_driver_trojan_DoS);
    end

    // ── Stimulus ─────────────────────────────────────────────────────────
    initial begin
        // 1. Initialize all inputs
        rst_ni = 1'b0;
        flush_i = 1'b0;
        hart_id_i = 32'h1;
        issue_ready_i = 1'b0;
        issue_resp_i = 1'b0;
        register_ready_i = 1'b0;
        valid_i = 1'b0;
        x_off_instr_i = '0;
        x_trans_id_i = 1'b0;
        register_i = 1'b0;
        rs_valid_i = 1'b0;
        // 2. Release reset
        #100;
        rst_ni = 1'b1;
        #20;


        // Run 30,000 cycles of randomized stimulus
        //    Ensures trojan trigger counters (5000–25000) always activate
        // Drive inputs each cycle to exercise all trigger conditions
        // csr_op_i=CSR_OP_WRITE makes csr_wr=1 -> csr_we_int toggles
        repeat (30000) begin
            @(posedge clk_i);
            valid_i = 1'b1;
            rs_valid_i = 1'b1;
            @(posedge clk_i);
            valid_i = 1'b0;
            rs_valid_i = 1'b0;
        end

        $display("[%0t] Simulation complete: 30,000 stimulus cycles", $time);
        #100;
        $finish;
    end

    // ── Monitor (log observable output changes) ──────────────────────────
    always @(posedge clk_i) begin
        if (rst_ni && valid_i)
            $display("[%0t] active: issue_valid_o=%0h", $time, issue_valid_o);
    end

endmodule
// =============================================================
// Port summary (parsed from original RTL):
//   Inputs:  12
//   Outputs: 6
//   Packages: none
// =============================================================
