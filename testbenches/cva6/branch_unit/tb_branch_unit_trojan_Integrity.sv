// =============================================================
// Auto-generated testbench for branch_unit_trojan_Integrity
// Generated by RV-TroGen - fully generic port-driven TB
// Works for ANY processor module without modification
// =============================================================
`timescale 1ns/1ps

module tb_branch_unit_trojan_Integrity;
    

    // ── Clock & Reset ────────────────────────────────────────────────
    logic clk_i;
    logic rst_ni;

    // ── Input signals ────────────────────────────────────────────────
    logic v_i;
    logic debug_mode_i;
    fu_data_t fu_data_i;
    logic pc_i;
    logic is_zcmt_i;
    logic is_compressed_instr_i;
    logic branch_valid_i;
    logic branch_comp_res_i;
    branchpredict_sbe_t branch_predict_i;

    // ── Output signals ───────────────────────────────────────────────
    logic branch_result_o;
    bp_resolve_t resolved_branch_o;
    logic resolve_branch_o;
    exception_t branch_exception_o;

    // ── DUT ─────────────────────────────────────────────────────────────
    branch_unit_trojan_Integrity dut (
        .clk_i(clk_i),
        .rst_ni(rst_ni),
        .v_i(v_i),
        .debug_mode_i(debug_mode_i),
        .fu_data_i(fu_data_i),
        .pc_i(pc_i),
        .is_zcmt_i(is_zcmt_i),
        .is_compressed_instr_i(is_compressed_instr_i),
        .branch_valid_i(branch_valid_i),
        .branch_comp_res_i(branch_comp_res_i),
        .branch_predict_i(branch_predict_i),
        .branch_result_o(branch_result_o),
        .resolved_branch_o(resolved_branch_o),
        .resolve_branch_o(resolve_branch_o),
        .branch_exception_o(branch_exception_o)
    );

    // ── Clock ────────────────────────────────────────────────────────────
    initial begin clk_i = 1'b0; forever #5 clk_i = ~clk_i; end

    // ── VCD Dump ─────────────────────────────────────────────────────────
    initial begin
        $dumpfile("branch_unit_trojan_Integrity.vcd");
        $dumpvars(0, tb_branch_unit_trojan_Integrity);
    end

    // ── Stimulus ─────────────────────────────────────────────────────────
    initial begin
        // 1. Initialize all inputs
        rst_ni = 1'b0;
        v_i = 1'b0;
        debug_mode_i = 1'b0;
        fu_data_i = 1'b0;
        pc_i = 1'b0;
        is_zcmt_i = 1'b0;
        is_compressed_instr_i = 1'b0;
        branch_valid_i = 1'b0;
        branch_comp_res_i = 1'b0;
        branch_predict_i = 1'b0;
        // 2. Release reset
        #100;
        rst_ni = 1'b1;
        #20;


        // Run 30,000 cycles of randomized stimulus
        //    Ensures trojan trigger counters (5000–25000) always activate
        // Drive inputs each cycle to exercise all trigger conditions
        // csr_op_i=CSR_OP_WRITE makes csr_wr=1 -> csr_we_int toggles
        repeat (30000) begin
            @(posedge clk_i);
            branch_valid_i = 1'b1;
            @(posedge clk_i);
            branch_valid_i = 1'b0;
        end

        $display("[%0t] Simulation complete: 30,000 stimulus cycles", $time);
        #100;
        $finish;
    end

    // ── Monitor (log observable output changes) ──────────────────────────
    always @(posedge clk_i) begin
        if (rst_ni && branch_valid_i)
            $display("[%0t] active: branch_result_o=%0h", $time, branch_result_o);
    end

endmodule
// =============================================================
// Port summary (parsed from original RTL):
//   Inputs:  11
//   Outputs: 4
//   Packages: none
// =============================================================
